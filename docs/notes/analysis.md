I have videos downloaded to some hard drives.
The file names for those videos contains the ID and a maybe a title and that title may not match the video to make it compatible with Windows.
We have teamy-mft to discover files with substrings that are identifiable as youtube video IDs.
We can map video ID to uploader
Some channels I have done a point-in-time bulk video download for.
We can determine what percentage/count of videos for each channel we have downloaded for.
If I've bulk synced a channel, we could auto-detect that maybe we want to sync them again.
Or just make a chart of "channels ranked by how many videos of theirs I have downloaded"
Or just make a chart of "channels ranked by how many minutes of video of theirs I have downloaded"